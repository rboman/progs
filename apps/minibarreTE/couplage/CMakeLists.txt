FILE(GLOB SRCS *.h *.cpp *.inl *.hpp)

ADD_LIBRARY(couplage SHARED ${SRCS})
IF(WIN32)
    SET_TARGET_PROPERTIES(couplage PROPERTIES DEBUG_POSTFIX "_d")
ENDIF()

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/gmm)
