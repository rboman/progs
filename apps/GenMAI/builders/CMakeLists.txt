# LIB

FILE(GLOB SRCS *.h *.cpp *.inl *.hpp)

ADD_LIBRARY(builders STATIC ${SRCS})

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/utils ${PROJECT_SOURCE_DIR}/params  
)

IF(WIN32)
    SET_TARGET_PROPERTIES(builders PROPERTIES DEBUG_POSTFIX "_d")
ENDIF(WIN32)
