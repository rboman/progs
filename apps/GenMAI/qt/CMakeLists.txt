
FIND_PACKAGE(Qt5 COMPONENTS Core Widgets REQUIRED)

FILE(GLOB UISRCS *.ui)
QT5_WRAP_UI(UISRCH ${UISRCS})

SET(CMAKE_AUTOMOC ON)
SET(CMAKE_INCLUDE_CURRENT_DIR ON)
#FIND_PACKAGE(Qt5Widgets REQUIRED)
#FIND_PACKAGE(Qt5Core REQUIRED)

FILE(GLOB SRCS *.h *.cpp)

ADD_EXECUTABLE(genmai_qt ${SRCS} ${UISRCS})
IF(MSVC)
    SET_TARGET_PROPERTIES(genmai_qt PROPERTIES DEBUG_POSTFIX "_d")
ENDIF(MSVC)

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/utils 
${PROJECT_SOURCE_DIR}/builders
${PROJECT_SOURCE_DIR}/export
${PROJECT_SOURCE_DIR}/params
)



QT5_USE_MODULES(genmai_qt Widgets Core)
TARGET_LINK_LIBRARIES(genmai_qt builders exporters params utils)
