# Exe 

FILE(GLOB SRCS *.h *.cpp)

ADD_EXECUTABLE(skytest ${SRCS})
IF(WIN32)
    SET_TARGET_PROPERTIES(skytest PROPERTIES DEBUG_POSTFIX "_d")
ENDIF(WIN32)

# ---

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/SKYLIB )

TARGET_LINK_LIBRARIES(skytest skylib )
