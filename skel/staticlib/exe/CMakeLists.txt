FILE(GLOB SRCS *.h *.cpp)

ADD_EXECUTABLE(skel ${SRCS})
IF(WIN32)
    SET_TARGET_PROPERTIES(skel PROPERTIES DEBUG_POSTFIX "_d")
ENDIF(WIN32)

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/lib )
TARGET_LINK_LIBRARIES(skel lib )
