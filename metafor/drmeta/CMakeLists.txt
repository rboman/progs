# Exe 
cmake_minimum_required(VERSION 2.6)
FILE(GLOB SRCS *.h *.c)

ADD_EXECUTABLE(drmeta ${SRCS})
IF(WIN32)
    SET_TARGET_PROPERTIES(drmeta PROPERTIES DEBUG_POSTFIX "_d")
ENDIF(WIN32)


#INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR} )

