# Exe 

FILE(GLOB SRCS *.h *.c)

ADD_EXECUTABLE(gausstest ${SRCS})
IF(WIN32)
    SET_TARGET_PROPERTIES(gausstest PROPERTIES DEBUG_POSTFIX "_d")
ENDIF(WIN32)

# ---

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/GAUSSLIB )

TARGET_LINK_LIBRARIES(gausstest gausslib )
