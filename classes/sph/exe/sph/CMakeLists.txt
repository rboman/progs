FILE(GLOB SRCS *.h *.cpp)
ADD_EXECUTABLE(sph ${SRCS})
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/vtl)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/tools)
TARGET_LINK_LIBRARIES(sph tools vtl)

add_test(NAME sph
         COMMAND ${CMAKE_BINARY_DIR}/bin/sph)
         