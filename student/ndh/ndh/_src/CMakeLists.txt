# CMake input file of the SWIG wrapper around "ndh.so"

INCLUDE(${SWIG_USE_FILE})

INCLUDE_DIRECTORIES(${Python3_INCLUDE_DIRS})

FILE(GLOB SRCS *.h *.cpp *.inl *.swg)
FILE(GLOB ISRCS *.i)

SET_SOURCE_FILES_PROPERTIES(${ISRCS} PROPERTIES CPLUSPLUS ON)

SET(SWINCFLAGS  -I${PROJECT_SOURCE_DIR}/ndh/src )
SET_SOURCE_FILES_PROPERTIES(${ISRCS} PROPERTIES SWIG_FLAGS "${SWINCFLAGS}")

SWIG_ADD_LIBRARY(ndhi LANGUAGE python SOURCES ${ISRCS} ${SRCS})
MACRO_DebugPostfix(_ndhi)

# ---

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/ndh/src )

SWIG_LINK_LIBRARIES(ndhi ndh ${Python3_LIBRARIES} )

